# -*- coding: utf-8 -*-

from setuptools import setup

setup(
    name='RestAPIInterface',
    packages=['RestAPIInterface'],
    include_package_data=True,
    install_requires=[
        'flask',
    ],
)
